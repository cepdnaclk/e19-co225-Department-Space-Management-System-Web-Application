package com.example.SharedSpaces.models.token;

// This enum represents the different types of authentication tokens that can be used.
public enum TokenType {
    BEARER // This value represents the Bearer token type, which is the most commonly used token type for authentication.
}